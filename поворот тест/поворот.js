const rotateButton = document.getElementById('rotateButton');
const box = document.getElementById('box');

rotateButton.addEventListener('click', () => {
    img.classList.toggle('rotated'); // Переключаем класс для поворота
});

function(){
    if(
        hit=1
    )
    {
        console.log("Попал")
        
    }
}